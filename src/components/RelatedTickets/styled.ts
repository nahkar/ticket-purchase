import styled from 'styled-components';
import { ThemeT } from '~styles/theme';

export const RelatedTickets__Wrapper = styled.div<ThemeT>``;
export const RelatedTickets__Title = styled.p<ThemeT>`
	font-size: 20px;
	line-height: 30px;
	font-weight: 700;
`;
